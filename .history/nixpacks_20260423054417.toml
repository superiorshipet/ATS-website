[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo_pgsql", "nodejs-20", "pnpm"]

[phases.install]
cmds = ["cd /app && pnpm install"]

[phases.build]
cmds = ["cd /app && pnpm build", "cp -r /app/dist/* /app/"]

[start]
cmd = "php -S 0.0.0.0:$PORT"
